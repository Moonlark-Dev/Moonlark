help_model:
  description: Model Management
  details: Manage OpenAI model configuration (superuser only)
  usage1: model (view model configuration)
  usage2: model <model_name> (change default model)
  usage3: model <model_name> <identify> (set model for specific app)
  usage4: model :default: <identify> (remove app configuration)

model:
  no_permission: "Permission denied: only superusers can use this command"
  set_default: "✅ Default model has been set to: {0}"
  set_identify: "✅ Model for app {0} has been set to: {1}"
  remove_identify: "✅ Model configuration for app {0} has been removed, will use default model"
  no_identify_config: "⚠️ App {0} has no specific model configuration"
  no_override: "(no special configuration)"
  models_list_failed: "⚠️ Failed to fetch model list: {0}"
  info: |
    📋 Model Configuration Info

    🔹 Default Model: {default_model}

    🔹 App-Specific Configuration:
    {override_list}

    🔹 Available Models:
    {models_list}

    📝 Usage:
      /model - Show this info
      /model <model_name> - Change default model
      /model <model_name> <identify> - Set model for specific app
      /model :default: <identify> - Remove app configuration
