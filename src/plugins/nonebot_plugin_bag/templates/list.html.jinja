<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <!-- MDB -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/7.2.0/mdb.min.css" rel="stylesheet" />
    <script type="text/javascript"
        src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/7.2.0/mdb.umd.min.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            background-image: url('https://www.dmoe.cc/random.php');

            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
            padding: 20px;
            padding-bottom: 0px;
        }

        .text-style {
            color: white;
            text-shadow: 0px 0px 4px black;
        }

        .text-purple {
            color: blueviolet;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="text-center text-style">{{ title }}</h1>
        <div class="card shadow bg-light bg-opacity-75">
            <div class="card-body">
                <div class="row">
                    <div class="col-6">
                        <span>{{ size }}</span>
                    </div>
                    <div class="col">
                        <div class="progress" style="height: 18px; margin-top: 3px;">
                            <div class="progress-bar" role="progressbar" style="width: {{ size_percent }};"></div>
                        </div>
                    </div>

                </div>
                <hr>
                <div class="row">
                    {% for item in items %}
                    <div class="col-6">
                        <span class="badge bg-info">{{ item.index }}.</span> <span
                            class="text-{{ item.text_color }} fw-bold">{{ item.name }}</span>
                        <span class="badge bg-light text-dark rounded-pill">{{ item.count }}</span>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
        <p class="text-center">{{ footer }}</p>
    </div>
</body>

</html>