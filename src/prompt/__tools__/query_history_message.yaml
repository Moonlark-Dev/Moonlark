description: |
  在当前会话的历史消息中查找包含特定关键词的信息。
  **何时调用**：
  - 当用户询问关于之前讨论过的内容时
  - 当你需要确认某个具体的时间点、发送者或对话细节时
  - 当你认为相关信息存储在历史消息数据库中而非即时记忆时
  **返回内容**：
  - 匹配的聊天记录列表，每条记录包含发送者、时间戳和具体内容。
parameters:
  - name: query
    description: "要搜索的关键词或短语。请尽可能使用简洁且具有辨识度的词汇。"
    type: string
    required: true
